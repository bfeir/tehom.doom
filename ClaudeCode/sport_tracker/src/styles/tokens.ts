// src/styles/tokens.ts
// Secondary artifact for JS/TS usage (e.g. setTimeout durations).
// These values mirror the CSS custom properties in design-tokens.css.

export const TRANSITION_DURATION = 150;
export const ANIMATION_DURATION = 220;
